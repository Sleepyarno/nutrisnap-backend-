# NutriSnap Learn Tab - Environment Variables
# Copy this file to .env and update with your actual values

# Firebase Service Account Key Path
# This should point to your Firebase service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/your-service-account-key.json

# Environment Selection
# Options: dev, test, prod
FIREBASE_ENV=dev

# Firebase Project IDs
# These should match your Firebase project IDs in each environment
FIREBASE_PROJECT_ID_DEV=nutrisnap-dev
FIREBASE_PROJECT_ID_TEST=nutrisnap-test
FIREBASE_PROJECT_ID_PROD=nutrisnap-prod

# Content Directory
# Path to the directory containing Markdown content files
CONTENT_DIR=../content
